# 용병단 이야기 — 마스터 로드맵

> 작성일: 2026-04-16
> 개정일: 2026-04-17 (v2 — 마일스톤 재배치, 데이터 분산, 스키마 통합 반영)
> 범위: 싱글 플레이어 컨텐츠 완성 (멀티플레이 제외)
> 목표: 전체 기능 구현 + 데이터 채우기 + 밸런싱

---

## 개정 이력

| 버전 | 일자 | 주요 변경 |
|------|------|---------|
| v1 | 2026-04-16 | 최초 작성. 6개 마일스톤 구조 |
| v2 | 2026-04-17 | Opus 기반 재검토로 다음 사항 반영 — ① M2 분할(M2a 아이템 인프라 / M2b 엘리트), ② 이동 이벤트 선택지를 M5 → M3 이동(템플릿 엔진 공유), ③ 용병 티어 업그레이드를 M5 → M6 이동(경제 충격 격리), ④ `quest_pools` 스키마 확장을 M1에서 일괄 수행, ⑤ 세력 전용 트레잇 데이터 모델 사전 확정 조항 추가, ⑥ M6 데이터 채우기를 각 마일스톤 종료 조건으로 분산, ⑦ 온보딩 작업을 M3~M6에 걸쳐 분산 |

---

## 현재 상태 요약

### 완료된 시스템

- 핵심 게임 루프 (모집 → 이동 → 파견 → 결과)
- 용병 시스템 (85개 직업, 5티어, STR/INT/VIT/AGI 4스탯)
- 트레잇 시스템 Phase 1~5 + Phase A + Phase B (109개 트레잇, 획득/진화/삭제/시설 연계)
- 시설 시스템 12종 (Lv25, 건설 큐, OGame 스타일)
- 이동 시스템 (199개 리전, 여행 이벤트 12종)
- 퀘스트 시스템 (4유형, 5난이도, 자동 갱신)
- 명성/랭크 시스템 (F~A, 티어 잠금 해제)
- 지역 조사 시스템 (지식 포인트, 발견 트리거)
- 세력 핵심 시스템 (14개 세력, 발견/가입/탈퇴/평판/충돌)
- 활동 로그, 방치형 보상, 시간 가속
- operation-bom 운영 도구 (18개 테이블 CRUD, 버전 관리)

### 미구현 시스템 (이 로드맵의 범위)

16개 시스템을 7개 마일스톤에 배치하여 순차 구현한다.

---

## 설계 원칙

### 마일스톤 운영 원칙

1. **매 마일스톤 = 기획 + 구현 + 데이터 + 밸런스 스냅샷** — 기능만 만들고 끝나지 않는다.
2. **기획자 선행** — 현재 마일스톤 구현 중에 기획자는 다음 마일스톤의 컨텐츠/데이터를 설계한다.
3. **마일스톤 간 밸런스 체크포인트** — 각 마일스톤 완료 시 balance-designer로 경제 시뮬레이션을 수행하고, 다음 마일스톤 진입 전 수치를 확정한다.
4. **통합 가능한 시스템은 통합** — trait Phase D(특수 임무)와 world expansion Phase 4(연계 퀘스트)는 M3에서 하나로 합친다. 서사 텍스트와 이동 선택지 이벤트도 M3에서 공통 템플릿 엔진으로 통합한다.
5. **릴리스 테마 기반** — 매 마일스톤이 플레이어 관점에서 의미 있는 변화를 전달한다.
6. **누적 플레이 가능 기준** — 각 마일스톤 종료 시점에 "게임을 처음부터 시작하여 해당 마일스톤까지의 경험을 연속으로 플레이할 수 있는 데이터 분량"을 함께 확보한다. 데이터 채우기를 M6에 몰지 않는다.

### 데이터 설계 원칙

- **구조화된 데이터는 JSONB 대신 정규화 테이블을 사용한다** — operation-bom에서 일반 CRUD 폼으로 편집할 수 있고, DB 레벨에서 검증이 가능하며, 기획자가 JSON 문법을 몰라도 된다.
- 기존에 동작하는 JSONB 필드(effect_json, discovery_data 등 스키마가 타입별로 가변적인 것)는 유지한다.
- **신규 추가하는 데이터는 정규화 우선.**
- **동일 테이블의 스키마 확장은 최초 1회에 몰아서 수행한다** — `quest_pools` 테이블에 필요한 `faction_tag`와 `sector_type` 필드는 M1에서 한 번에 추가하고, M3은 데이터만 채우도록 한다. table-config·Freezed 모델·SyncService 마이그레이션을 중복하지 않는다.

### 우선순위 기준

- **의존 관계를 기본 골격으로** — 다른 시스템의 선행 조건이 되는 것을 먼저 배치한다.
- **독립 기능은 게임 경험 임팩트 순으로 배치** — 의존 체인 사이의 빈 슬롯에 끼워넣는다.
- **경제 충격이 큰 변경은 격리한다** — 용병 티어 업그레이드처럼 전체 골드 수급·지출 곡선을 재설계하는 변경은 단독 마일스톤(M6)에서 다룬다. 경제 충격과 다른 기능 변경이 뒤섞이면 밸런스 이상의 원인 분리가 불가능하다.

---

## 마일스톤 전체 구조

```
M1  "세력의 영향력"       ── 세력이 실제 게임에 영향을 미치기 시작
  ↓
M2a "아이템의 태동"       ── 아이템 인프라 + 정수(영구 스탯 강화) 경로 개시
  ↓
M2b "엘리트의 시대"       ── 엘리트 몬스터 + 드랍 테이블, 반복 파밍 성립
  ↓
M3  "깊어지는 세계"       ── 연계 퀘스트 + 지역 변형 + 서사/선택지 통합 텍스트 계층
  ↓
M4  "세력의 심화"         ── 전용 시설·거점·상점·트레잇으로 세력 완결
  ↓
M5  "용병의 유대"         ── 용병 간 관계 트레잇 (순수 전략 깊이, 경제 변경 없음)
  ↓
M6  "완성과 균형"         ── 티어 업그레이드 + 전체 데이터 채우기 + 밸런싱 + 최종 폴리싱
```

### 의존 관계

```
M1 (세력 영향력)  ──→ M4 (세력 심화)          : 패시브/태그가 있어야 시설/상점이 의미를 지닌다
M2a (아이템 인프라) ──→ M2b (엘리트 드랍)     : 아이템 모델·인벤토리가 있어야 드랍 판정이 가능하다
M2a (아이템 인프라) ──→ M3 (연계 퀘스트 보상) : 아이템이 있어야 연계 퀘스트 최종 보상이 성립한다
M2a (아이템 인프라) ──→ M4 (세력 상점 상품)   : 아이템이 있어야 상점에서 판매할 상품이 존재한다
M1~M5 전체         ──→ M6 (완성과 균형)      : 모든 시스템이 있어야 티어 업그레이드 경제가 설계된다
```

M5(용병 유대)는 독립 작업이며 M2a 이후 언제든 착수 가능하지만, 플레이어 동기부여 흐름상 M4 이후로 배치한다.

### 16개 시스템의 마일스톤 배치

| # | 시스템 | 마일스톤 | 근거 |
|---|--------|---------|------|
| 14 | 세력 패시브 효과 실제 연동 | M1 | 데이터가 이미 있고 게임 효과만 연결하면 된다 |
| 5 | 세력 퀘스트 태그 + 전용 퀘스트 | M1 | 세력이 게임 루프에 진입하는 핵심 |
| 12 | 파견 상성 시스템 | M1 | 독립적, 전투 깊이 추가 |
| 13 | 명성 등급별 보너스 | M1 | 독립적, 소규모 작업 |
| 1 | 아이템/장비 인프라 | M2a | M2b/M3/M4의 선행 조건 |
| 2 | 엘리트 몬스터 | M2b | 아이템 드랍의 핵심 경로 |
| 3 | 숨겨진 연계 퀘스트 | M3 | 아이템 보상 필요(M2a 후) |
| 9 | 특수 임무 시스템 | M3 | #3과 통합 |
| 4 | 지역 변형 | M3 | 지역 조사의 최종 보상 |
| 11 | 퀘스트 서사 텍스트 | M3 | 연계 퀘스트와 시너지, 선택지와 템플릿 엔진 공유 |
| 10 | 이동 이벤트 선택지 | M3 | 서사 텍스트와 동일 템플릿/편집기 파이프라인 |
| 6 | 세력 전용 시설 14종 | M4 | M1 패시브 + M2a 아이템 필요 |
| 7 | 세력 거점 + 상점 + 전용 트레잇 | M4 | 아이템이 상점 상품, 트레잇 데이터 모델 사전 확정 필요 |
| 8 | 용병 간 상호작용 | M5 | 독립적, 파견 전략 깊이, 경제 영향 없음 |
| 16 | 용병 티어 업그레이드 | M6 | 경제 재설계 필요, 전체 밸런스와 함께 수행 |
| 15 | 밸런스 튜닝 + 데이터 채우기 | M6 | 전체 시스템 완성 후 최종 패스 |

---

## M1: 세력의 영향력

### 목표

현재 세력 시스템은 "발견하고 가입하면 텍스트가 보이는" 수준이다. M1이 끝나면 세력 가입이 퀘스트 보상, 성공률, 경제 수치에 실제로 영향을 미친다. 플레이어가 "어떤 세력에 가입할까"를 진지하게 고민하기 시작하는 지점이다.

### 포함 시스템

| 시스템 | 핵심 변화 |
|--------|----------|
| **세력 패시브 효과 연동** | `passive_bonus_json`이 실제 게임 수치에 반영된다 (보상 배수, 성공률, 회복 시간 등) |
| **세력 퀘스트 태그 + 전용 퀘스트** | 일반 퀘스트에 세력 태그가 부여되며 완료 시 평판을 획득한다. 가입 세력 전용 고난도 퀘스트가 생성된다 |
| **파견 상성 시스템** | 퀘스트 유형별 유리한 직업/트레잇 조합이 존재하며 파견 선택에 전략적 깊이를 더한다 |
| **명성 등급별 보너스** | 랭크 상승 시 해금 외에 추가 보너스(보상 +%, 모집 할인 등)를 제공한다 |

### 작업 상세

**기획 (content-designer / balance-designer)**

- 14개 세력의 패시브 보너스를 게임 메커니즘에 매핑하는 효과 타입을 정의한다.
- 세력 태그 퀘스트의 확률 테이블을 설계한다 (거점 거리별 보정 포함).
- 세력 전용 퀘스트의 난이도/보상을 설계한다 (일반 대비 +1~2 난이도, 평판 +5~10).
- 상성 매트릭스를 설계한다: 직업군 × 퀘스트 유형별 보정값.
- 명성 등급(F~A)별 보너스 수치를 확정한다.

**구현 (Flutter — band_of_mercenaries)**

- `PassiveBonusService`를 신규 추가한다. 패시브 보너스 데이터를 조회하여 기존 서비스(QuestCalculator, RecruitmentService, 회복 로직 등)에 보정값을 주입한다.
- `QuestGenerator`를 확장한다. 퀘스트 생성 시 세력 태그 확률을 판정하고 가입 세력 전용 퀘스트 슬롯을 관리한다.
- `QuestCompletionService`를 확장한다. 태그 퀘스트 완료 시 `FactionStateRepository.addReputation()`을 호출한다.
- `QuestCalculator`를 확장한다. 직업/트레잇 × 퀘스트 유형 상성 보정을 적용한다.
- `ReputationService`를 확장한다. 랭크별 보너스를 적용한다.
- 파견 화면에 세력 태그 배지와 상성 힌트 UI를 추가한다.

**운영 도구 (operation-bom)**

- Supabase에 신규 테이블을 생성한다:
  - `faction_passive_bonuses` (기존 JSONB 필드를 정규화 테이블로 전환)
  - 상성 테이블 (`quest_synergy_matrix` 등, 신규 테이블 사용 시)
- **`quest_pools` 스키마를 한 번에 확장한다** — `faction_tag` 필드와 `sector_type` 필드(village/ruins/hidden, 기본 null)를 M1에서 동시에 추가한다. `sector_type` 데이터 입력은 M3에서 수행하지만, 스키마·Freezed 모델·SyncService·table-config 마이그레이션은 M1에서 일괄로 끝낸다.
- `ranks` 테이블에 보너스 필드를 추가한다.
- `table-config.ts`에 신규 테이블과 확장 필드를 반영한다.
- 데이터 입력 후 `data_versions`를 퍼블리시한다.

**데이터 입력 (operation-bom에서 수행)**

- 14개 세력의 패시브 보너스 데이터를 입력한다 (`faction_passive_bonuses` 테이블).
- 세력 전용 퀘스트 풀을 추가한다 (세력당 3~5개, 총 42~70개).
- 상성 보정 데이터를 입력한다.
- `ranks` 테이블: 등급별 보너스 값을 입력한다.

**밸런스 체크 (balance-designer)**

- 세력 패시브가 경제에 미치는 영향을 시뮬레이션한다 (보상 +15%가 골드 인플레이션을 유발하는지).
- 상성 보정이 성공률 분포를 어떻게 바꾸는지 검증한다.
- 명성 보너스의 누적 효과를 검증한다.

### 누적 플레이 기준 (M1 종료 시)

- 신규 유저가 F랭크에서 시작하여 세력 단서 발견 → 가입 → 전용 퀘스트 수행 → 평판 누적 → 랭크 업까지 30~60분 연속 플레이가 가능한 수준으로 세력 전용 퀘스트 풀이 채워져 있어야 한다.

### 완료 점검표

**기능 동작**

- [ ] 세력 가입 후 퀘스트 보상이 패시브 보너스만큼 실제로 변하는가?
- [ ] 세력 단서를 보유한 리전에서 퀘스트 생성 시 태그가 확률적으로 붙는가?
- [ ] 태그 퀘스트 완료 → 평판 +1~2 → 도감에서 평판 바 변화 확인
- [ ] 가입 세력의 전용 퀘스트가 파견 화면에 별도 표시되는가?
- [ ] 파견 화면에서 유리한 용병 조합에 대한 시각적 힌트가 보이는가?
- [ ] 랭크 E 이상에서 해당 등급의 보너스가 적용되는가?

**데이터 & 운영 도구**

- [ ] operation-bom에서 세력 전용 퀘스트를 생성/수정/삭제할 수 있는가?
- [ ] `quest_pools`에 `faction_tag`와 `sector_type` 필드가 확장되어 있고 편집 가능한가?
- [ ] operation-bom에서 상성 데이터를 편집할 수 있는가?
- [ ] operation-bom에서 랭크 보너스를 편집할 수 있는가?
- [ ] data_versions 퍼블리시 후 Flutter 앱에서 신규 데이터가 정상 동기화되는가?

**밸런스 & 회귀**

- [ ] balance-designer로 경제 시뮬레이션 1회 이상 수행, 이상치 없음 확인
- [ ] 세력 미가입 상태에서 기존 게임 루프가 이전과 동일하게 동작

### M1 완료 후 게임 상태

> 플레이어가 세력에 가입하면 퀘스트 보상이 실제로 달라진다. 특정 세력 근처에서 태그 퀘스트를 수행하며 평판을 올릴 수 있고, 충분한 평판이 쌓이면 전용 고난도 퀘스트에 도전할 수 있다. 파견 시 "이 퀘스트에는 어떤 용병이 유리한가"를 고려하는 전략적 깊이가 생긴다. 명성이 올라갈수록 추가 보너스가 붙어 성장 동기가 강화된다. 운영팀은 operation-bom에서 세력 퀘스트, 상성 데이터, 랭크 보너스를 자유롭게 편집하고 앱에 반영할 수 있다.

---

## M2a: 아이템의 태동

### 목표

게임에 골드 이외의 새로운 보상 축을 도입하는 인프라를 깐다. 아이템 모델, 인벤토리, 장비 장착/해제, 정수 사용(영구 스탯 강화)까지를 구현한다. 드랍 경로는 없지만 operation-bom에서 수동 지급으로 검증이 가능하며, 인플레이션 곡선을 사전에 시뮬레이션할 수 있다.

### 분할 근거

v1에서는 M2에 아이템 인프라와 엘리트 몬스터를 묶었다. 두 영역은 각각 다음과 같은 작업을 포함한다.

- 아이템 인프라: Freezed 모델 3종, Hive 박스 신설, effective 스탯 재계산, 인벤토리 UI, 용병 장비 슬롯 UI, 정수 소모 연출
- 엘리트 파이프라인: 엘리트 출현 로직, 퀘스트 생성 분기, 전투 난이도 조정, 드랍 엔진, 반복 파밍 설계

둘이 같은 마일스톤이면 인프라를 검증할 아이템 출처가 없고, 드랍 엔진을 검증할 아이템 풀이 없어 교차 테스트가 막힌다. 분할하면 M2a 종료 시점에 정수 인플레이션을 선행 검증하고, M2b 작업 범위를 드랍 엔진에 집중할 수 있다.

### 포함 시스템

| 시스템 | 핵심 변화 |
|--------|----------|
| **아이템/장비 인프라** | 아이템 모델, 인벤토리 Hive 박스, 장비 장착/해제, 정수 사용(영구 스탯 강화), 인벤토리 UI |

### 작업 상세

**기획 (content-designer / balance-designer)**

- 아이템 분류 체계를 확정한다: 개인 장비(무기/방어구/장신구) + 용병단 장비(군기/유물) + 소모품(정수/일반).
- 정수 종류 및 영구 스탯 강화 수치를 설계한다 (힘/수호/생명/상급).
- 장비 스탯 보정 범위, 용병단 장비 전체 효과 범위를 정한다.
- 검증용 최소 데이터 세트: 정수 4종, 개인 장비 6~8종, 용병단 장비 3~4종.

**구현 (Flutter — band_of_mercenaries)**

- `ItemData` 정적 모델(Freezed)을 작성한다: id, name, category, slot, tier, description 등.
- `InventoryItem` Hive 모델을 작성한다: itemId, quantity, equippedTo(용병 ID).
- `inventory` Hive 박스를 신설한다.
- `InventoryRepository`를 작성한다: 아이템 추가/제거/장착/해제 CRUD.
- `ItemEffectService`를 작성한다: 장착 장비의 스탯 보정을 기존 effectiveStr/effectiveAgi 등에 반영한다.
- `EssenceService`를 작성한다: 정수 사용 시 용병 영구 스탯이 증가하고 정수가 소멸한다.
- 인벤토리 UI: 아이템 목록 화면(카테고리별 필터), 아이템 상세 팝업.
- 용병 상세 오버레이에 장비 슬롯(무기/방어구/장신구)을 추가한다.
- `SyncService`를 확장한다: `items` 테이블 동기화.

**운영 도구 (operation-bom)**

- Supabase에 `items` 테이블을 생성한다.
- `table-config.ts`에 `items` 정의를 추가한다. category/slot/tier 셀렉트 필드를 지원한다.
- **검증용 수동 지급 인터페이스를 추가한다** — 특정 유저에게 아이템을 직접 추가하는 관리자 액션을 구현한다. 드랍 엔진(M2b) 없이 인벤토리·장착·정수 소모 전체 경로를 검증할 수 있다.
- `data_versions`에 행을 추가한다.

**데이터 입력 (operation-bom에서 수행)**

- 정수 4종(힘/수호/생명/상급)을 입력한다.
- 개인 장비 6~8종(티어 2~5 분포)을 입력한다.
- 용병단 장비 3~4종을 입력한다.

**밸런스 체크 (balance-designer)**

- 정수 누적 사용 시 영구 스탯 인플레이션 시뮬레이션 — 정수 N개 획득 가정 시 스탯 상승폭 × 성공률 변화율.
- 장비 스탯 보정이 기존 성공률 밸런스를 얼마나 변경하는지 측정.
- 용병단 장비가 전체 용병에게 적용될 때의 총합 영향.

### 누적 플레이 기준 (M2a 종료 시)

- M1 범위의 플레이에 더해, operation-bom에서 정수·장비를 수동 지급받은 가정 아래 용병 장비 세팅을 실험할 수 있어야 한다. 엘리트가 없어도 "만약 정수 10개를 모으면 어떤 성장 곡선이 되는가"를 시뮬레이션할 수 있다.

### 완료 점검표

- [ ] 인벤토리 화면에서 보유 아이템 목록을 카테고리별로 확인할 수 있다.
- [ ] 용병 상세 오버레이에 장비 슬롯이 보이고 장착/해제가 동작한다.
- [ ] 장비 장착 시 용병의 effective 스탯이 실제로 변한다.
- [ ] 정수를 용병에게 사용하면 영구 스탯이 증가하고 정수가 소멸한다.
- [ ] 용병단 장비 장착 시 전체 용병에게 효과가 적용된다.
- [ ] 앱 재시작 후 인벤토리와 장착 상태가 Hive에서 정상 복원된다.
- [ ] operation-bom에서 아이템을 생성/수정/삭제할 수 있다.
- [ ] operation-bom의 수동 지급 인터페이스로 정수·장비를 유저에게 전달할 수 있다.
- [ ] balance-designer로 정수 사용 인플레이션 시뮬레이션 완료.
- [ ] M1 기능 회귀 이상 없음.

### M2a 완료 후 게임 상태

> 용병마다 무기·방어구·장신구 슬롯이 열려 있다. 플레이어는 아직 아이템을 스스로 획득할 수는 없지만, 인벤토리 화면과 장착 UI가 완비되어 있다. 용병단 유물을 장착하면 전체 전력이 상승한다. 운영팀은 테스트 단계에서 아이템을 수동 지급하여 장비 성장 곡선을 검증한다. 다음 M2b에서 엘리트 몬스터가 첫 번째 공식 드랍 경로로 열린다.

---

## M2b: 엘리트의 시대

### 목표

M2a의 아이템 인프라 위에 엘리트 몬스터를 올린다. 지역 조사로 엘리트의 존재를 해금하고, 강한 용병을 파견하여 토벌하고, 드랍 판정으로 정수·장비·용병단 유물을 획득한다. M2b가 끝나면 "이 지역에 머물며 강철 골렘을 반복 사냥해서 힘의 정수를 모은다"는 새로운 플레이 패턴이 성립한다.

### 선행 조건

- M2a 아이템 인프라 — 드랍 엔진이 인벤토리에 아이템을 넣을 수 있어야 한다.
- 지역 조사 시스템 (완료)
- `region_discoveries.discovery_type = elite` 인프라 (완료)

### 포함 시스템

| 시스템 | 핵심 변화 |
|--------|----------|
| **엘리트 몬스터** | 지식 임계값으로 해금, 반복 출현, 고난도 토벌 퀘스트, 드랍 판정 |

### 작업 상세

**기획 (content-designer / balance-designer)**

- 엘리트 몬스터 카탈로그를 설계한다: 리전 티어별 전투력, 출현 확률, 보상 구성.
- 드랍 테이블 가이드라인: 아이템별 드랍률(정수 40~60%, 장비 15~25%, 용병단 장비 3~8%).
- 검증용 최소 데이터: 엘리트 10~15종.

**구현 (Flutter — band_of_mercenaries)**

- `EliteMonsterData` 정적 모델(Freezed)을 작성한다.
- `EliteLootEntry` 정적 모델을 작성한다: elite_id, item_id, drop_rate.
- 엘리트 출현 로직 — `RegionState.triggeredDiscoveries`에 엘리트 discovery가 존재하면 퀘스트 갱신 시 확률 판정(티어별 8~15%)으로 엘리트 퀘스트를 생성한다.
- 엘리트 퀘스트 — 기존 `ActiveQuest`에 `isElite` 플래그를 추가하고, 전투력 100~200, 소요시간 1.5~2배로 설정한다.
- 드랍 판정 — 퀘스트 완료 시 `EliteLootService`가 각 드랍 항목별 독립 확률 판정으로 인벤토리에 아이템을 추가한다.
- 파견 화면에 엘리트 퀘스트 시각적 구분을 추가한다 (아이콘, 강조 색상, 최소 파견 인원 권장 표시).
- 퀘스트 완료 팝업에 아이템 드랍 결과를 표시한다.
- `SyncService`를 확장한다: `elite_monsters`, `elite_loot_tables` 테이블 동기화.

**운영 도구 (operation-bom)**

- Supabase에 `elite_monsters`, `elite_loot_tables` 테이블을 생성한다.
- `table-config.ts`에 2개 테이블 정의를 추가한다.
- `elite_loot_tables` 편집 화면: elite_id와 item_id는 이름 참조 표시, drop_rate는 0.0~1.0 범위 검증.
- `data_versions`에 행을 추가한다.

**데이터 입력 (operation-bom에서 수행)**

- 엘리트 몬스터 10~15종(리전 티어별 분포)을 입력한다.
- 드랍 테이블 — 엘리트별 드랍 항목 및 확률.
- `region_discoveries`에 엘리트 관련 discovery 데이터를 추가한다.

**밸런스 체크 (balance-designer)**

- 엘리트 전투력 vs 플레이어 파티 전투력 분포.
- 드랍률 × 엘리트 출현 확률 × 파밍 주기 — 아이템 획득 기대 시간 검증.
- M2a에서 시뮬레이션한 정수 인플레이션에 엘리트 파밍 속도를 대입하여 실제 현실 수치를 재조정한다.

### 누적 플레이 기준 (M2b 종료 시)

- 신규 유저가 M1 흐름을 거친 후 Tier 2~3 리전에서 엘리트 1~2종을 해금, 반복 사냥으로 정수 5~10개를 모아 용병 1명의 영구 스탯을 의미 있게 올리는 3~5시간 플레이가 가능해야 한다. 이를 위해 Tier 2~3 리전 일부에 엘리트 데이터가 배치되어 있어야 한다.

### 완료 점검표

- [ ] 지식 임계값 도달 후 해당 리전에서 퀘스트 갱신 시 엘리트 퀘스트가 확률적으로 출현한다.
- [ ] 엘리트 퀘스트가 파견 화면에서 일반 퀘스트와 시각적으로 구분된다.
- [ ] 엘리트 토벌 성공 시 드랍 판정이 돌아가고 획득 아이템이 인벤토리에 추가된다.
- [ ] 퀘스트 완료 팝업에 드랍된 아이템이 표시된다.
- [ ] 엘리트를 처치해도 이후 퀘스트 갱신에서 다시 확률적으로 출현한다 (반복 파밍 가능).
- [ ] operation-bom에서 엘리트 몬스터와 드랍 테이블을 편집할 수 있다.
- [ ] data_versions 퍼블리시 후 Flutter 앱에서 2개 테이블 데이터가 정상 동기화된다.
- [ ] balance-designer로 정수 파밍 실제 수치 시뮬레이션 완료, M2a 시뮬레이션과 교차 검증.
- [ ] M1 기능 회귀 이상 없음.

### M2b 완료 후 게임 상태

> 플레이어는 지역 조사로 엘리트 몬스터의 존재를 발견하고, 강력한 용병을 파견하여 토벌에 도전한다. 성공하면 정수, 장비, 드물게는 용병단 유물을 획득한다. 정수로 용병의 영구 스탯을 올리고, 장비를 장착하여 더 어려운 엘리트에 도전하는 새로운 성장 루프가 생긴다. "이 리전에 머물며 강철 골렘을 반복 사냥해서 힘의 정수를 모을까, 아니면 다음 리전으로 넘어갈까"라는 전략적 판단이 등장한다.

---

## M3: 깊어지는 세계

### 목표

지역 조사의 최종 보상을 완성한다. 지식을 쌓으면 다단계 서사 퀘스트가 열리고, 끝까지 조사하면 지역 자체가 변형된다. 퀘스트 결과와 이동 중 이벤트에 서사 텍스트가 붙어 세계가 살아있는 느낌을 준다. M3이 끝나면 "이 지역을 끝까지 파고들면 어떤 이야기가 나올까"라는 호기심이 게임을 이끄는 동력이 된다.

### 포함 시스템

| 시스템 | 핵심 변화 |
|--------|----------|
| **숨겨진 연계 퀘스트** | 지식 임계값 도달 시 2~5단계 체인 퀘스트 발동. 지역 이동 포함 가능. 최종 보상으로 확정 아이템. trait Phase D(특수 임무)를 통합 |
| **지역 변형** | 지식 최고 임계값 도달 시 섹터가 마을/유적지/숨겨진 섹터로 변형. 변형된 섹터에 전용 퀘스트 풀 |
| **퀘스트 서사 + 이동 선택지 (통합 텍스트 계층)** | 퀘스트 결과마다 조합형 서사 텍스트 생성. 이동 완료 시 확률적으로 선택지 이벤트 발동. 두 영역은 `TemplateEngine` 공유 모듈을 통해 변수 치환·조건 분기·트레잇 기반 숨겨진 옵션을 동일한 방식으로 처리 |

### 통합 근거 (서사 + 선택지)

v1에서는 퀘스트 서사 텍스트를 M3에, 이동 이벤트 선택지를 M5에 분리 배치했다. 그러나 두 영역은 다음을 공유한다.

- 변수 치환(용병 이름, 직업명, 지역명)
- 트레잇/스탯 기반 숨겨진 분기
- operation-bom 템플릿 편집기
- 활동 로그 연동

이를 한 마일스톤에 묶으면 `TemplateEngine` 공유 모듈을 1회 설계하고 두 영역이 재사용한다. 분리하면 편집기·검증 툴·조건 분기 엔진을 두 번 만들게 된다.

### 선행 조건

- M2a 아이템 인프라 — 연계 퀘스트 최종 보상, 유적지 전용 퀘스트 드랍에 아이템을 사용한다.
- 지역 조사 + `region_discoveries` (완료) — `discovery_type: hidden_quest`, `discovery_type: transform` 트리거.

### 작업 상세

**기획 (content-designer)**

- 체인 퀘스트 시나리오 5~8개(리전 티어별 분포), 2~5단계, 단계별 수행 지역/전투력/보상/서사 텍스트.
- 지역 이동을 포함하는 체인의 서사 흐름, 실패 시 재도전 가능 설계.
- 변형 대상 리전 15~25개 선정 (마을/유적지/숨겨진 섹터 유형별 컨텐츠).
- 서사 템플릿: 퀘스트 유형(4) × 결과(4) 매트릭스, 유형×결과별 3~5개 변형, 총 48~80개.
- 이동 선택지 이벤트 시나리오 10~15종, 선택지 2~3개, 선택지별 결과 확률, 트레잇 기반 숨겨진 선택지.
- **공존 정책 정의** — 가입 세력 전용 퀘스트(M1)와 연계 퀘스트(M3)가 동시에 존재할 때 파견 화면 정렬 순서·강조 방식·UI 슬롯 규칙을 확정한다. 기본 원칙: 연계 진행 중 단계는 최상단 고정, 그 아래 가입 세력 전용, 그 아래 엘리트, 나머지는 일반 퀘스트.

**구현 (Flutter — band_of_mercenaries)**

공유 텍스트 엔진:
- `TemplateEngine` 모듈 신규 — 변수 치환 `{merc_name}/{job}/{region}`, 조건 분기, 트레잇 기반 숨겨진 옵션 평가. M3 내 서사와 선택지가 모두 이 엔진을 호출한다.

연계 퀘스트:
- `ChainQuestData` 정적 모델(Freezed): chain_id, region_id, step, target_region_id, name, description, combat_power, duration_minutes, reward_gold, reward_items, final_reward, next_step_delay.
- `ChainQuestProgress` Hive 모델: chainId, currentStep, status(active/completed), startedAt.
- `chainQuestProgress` Hive 박스를 신설한다.
- `ChainQuestService` — 발견 트리거 시 1단계 자동 생성, 단계 완료 시 다음 단계 생성, 최종 단계에서 확정 보상.
- 파견 화면 — 연계 퀘스트를 상단 고정, 강조 색상, 진행도 표시, 타 지역 "이동 필요" 안내.
- 실패 시 해당 단계 재도전 가능.
- `SyncService` 확장 — `chain_quests` 테이블 동기화.

지역 변형:
- `RegionState` 모델 확장 — `sectorChanges` (Map<int, String>) 추가.
- 지식 임계값 도달 + transform 트리거 시 변형을 기록한다.
- 이동 화면에서 변형된 섹터를 특수 아이콘/이름으로 구분한다.
- `QuestGenerator` 확장 — 섹터 변형 타입에 따라 전용 퀘스트 풀에서 생성 (M1에서 추가한 `sector_type` 필드 활용).
- 변형 발견 시 팝업과 활동 로그 기록.

퀘스트 서사:
- `QuestNarrativeData` 정적 모델(Freezed): quest_type, result_type, template_text.
- `QuestNarrativeService` — 완료 시 템플릿 조회 → `TemplateEngine` 치환 → 서사 텍스트 출력.
- 퀘스트 완료 팝업에 서사 텍스트 영역을 추가한다.
- `SyncService` 확장 — `quest_narratives` 테이블 동기화.

이동 선택지:
- `TravelChoiceEventData` 정적 모델(Freezed).
- `TravelEventService` 확장 — 선택지 이벤트 풀 확률 판정.
- 이벤트 발생은 이동 완료 시 "회상" 형식으로 제시한다 (이동 중단 없음).
- 선택지 UI — 상황 텍스트 + 선택 버튼, 결과 즉시 적용 + 활동 로그.
- 트레잇 기반 숨겨진 선택지는 `TemplateEngine`이 평가한다.
- `SyncService` 확장 — `travel_choice_events` 테이블 동기화.

온보딩:
- 연계 퀘스트 최초 발동 시 튜토리얼 팝업 1회 표시 ("이 퀘스트는 여러 단계로 이어집니다").
- 첫 선택지 이벤트 조우 시 안내 한 줄.

**운영 도구 (operation-bom)**

- Supabase에 신규 테이블 3개 — `chain_quests`, `quest_narratives`, `travel_choice_events`.
- `table-config.ts`에 3개 테이블 정의를 추가한다.
- `chain_quests` 편집 — chain_id로 그룹핑하여 체인 단위 조회/편집.
- `quest_narratives` 편집 — quest_type × result_type 필터, 템플릿 변수 미리보기.
- `travel_choice_events` 편집 — 선택지별 결과 확률 인라인 편집, 확률 합산 검증.
- `data_versions`에 신규 테이블 행을 추가한다.

**데이터 입력 (operation-bom에서 수행)**

- 연계 퀘스트 체인 5~8개 (총 15~30단계).
- `region_discoveries`에 hidden_quest + transform 트리거 추가.
- 변형 대상 15~25개 리전의 transform 데이터.
- 마을/유적지 전용 퀘스트 풀 (유형별 10~15개, `quest_pools`의 `sector_type` 필드 활용).
- 서사 템플릿 48~80개.
- 이동 선택지 이벤트 10~15종.
- 연계 퀘스트 최종 보상 아이템이 items 테이블에 존재하는지 확인한다.

**밸런스 체크 (balance-designer)**

- 연계 퀘스트 확정 보상 강도 vs 엘리트 반복 파밍 시간 효율 비교.
- 유적지 전용 퀘스트 난이도/보상 프리미엄 적절성.
- 마을 퀘스트 보상이 "안전한 대안"으로서 적절한 수준인지.
- 이동 선택지 기대값 — 위험/안전 선택 모두 합리적인지.

### 누적 플레이 기준 (M3 종료 시)

- 신규 유저가 M1~M2b를 거친 후 연계 퀘스트 2개 이상을 완주하고, 지역 변형을 1회 이상 경험하며, 이동 중 선택지 이벤트를 여러 번 겪는 5~8시간 플레이가 가능해야 한다. 서사 템플릿이 모든 quest_type × result_type 조합에 최소 1개씩 존재한다.

### 완료 점검표

**연계 퀘스트**

- [ ] 지식 임계값 도달 시 1단계 연계 퀘스트가 자동 생성된다.
- [ ] 파견 화면에서 연계 퀘스트가 상단 고정, 진행도 표시된다.
- [ ] 타 지역 수행 단계에서 "이동 필요" 안내, 해당 지역 도착 후 파견 가능하다.
- [ ] 단계 실패 시 해당 단계만 재도전 가능하다.
- [ ] 최종 단계 완료 시 확정 아이템이 인벤토리에 추가된다.
- [ ] 완료된 체인은 재발동이 방지된다.
- [ ] 가입 세력 전용 퀘스트(M1)와 연계 퀘스트가 동시에 파견 목록에 있을 때 공존 정책대로 정렬된다.

**지역 변형**

- [ ] 지식 최고 임계값 도달 시 변형 팝업과 활동 로그가 기록된다.
- [ ] 이동 화면에서 변형된 섹터가 아이콘/이름으로 시각적 구분된다.
- [ ] 마을/유적지/숨겨진 섹터에서 각각 전용 퀘스트가 생성된다.

**퀘스트 서사 + 이동 선택지**

- [ ] 퀘스트 완료 팝업에 서사 텍스트가 표시된다.
- [ ] 서사 텍스트에 용병 이름, 직업명, 지역명이 정확히 치환된다.
- [ ] 동일 유형/결과 조합에서 여러 변형 중 랜덤으로 선택된다.
- [ ] 이동 완료 시 확률적으로 선택지 이벤트가 표시된다.
- [ ] 선택지별 결과(골드/아이템/평판/상태)가 즉시 반영된다.
- [ ] 특정 트레잇 보유 시 숨겨진 선택지가 추가 표시된다.
- [ ] 기존 자동 여행 이벤트(12종)와 선택지 이벤트가 공존한다.

**데이터 & 운영 도구**

- [ ] operation-bom에서 연계 퀘스트를 체인 단위로 조회/생성/수정할 수 있다.
- [ ] operation-bom에서 서사 템플릿을 유형×결과로 필터링하여 편집할 수 있다.
- [ ] operation-bom에서 선택지 이벤트의 결과 확률 합산이 검증된다.
- [ ] data_versions 퍼블리시 후 Flutter 앱에서 신규 데이터 정상 동기화 확인.

**밸런스 & 회귀**

- [ ] balance-designer로 연계 퀘스트 보상 vs 엘리트 파밍 시간 효율 비교 수행.
- [ ] M1 + M2a + M2b 기능 회귀 이상 없음.

### M3 완료 후 게임 상태

> 지역을 깊이 조사하면 숨겨진 이야기가 열린다. "봉인 문서 해독" 연계 퀘스트를 따라 대륙을 횡단하며 3단계 서사를 진행하면, 마지막에 고대의 유물을 확정으로 얻는다. 리전을 끝까지 파면 폐광이 마을로 바뀌거나, 안개가 걷히며 고대 유적이 드러난다. 변형된 섹터에서는 전용 퀘스트가 생성되어 새로운 도전거리가 생긴다. 모든 퀘스트 결과에 "용병 김철수가 적 대장과 일대일 결투를 벌여 승리했다" 같은 서사가 붙고, 이동 중에는 "부상당한 여행자" 이벤트를 만나 도와줄지·무시할지·소지품을 뒤질지를 선택한다. 숫자 나열이 아닌 이야기로 느껴진다.

---

## M4: 세력의 심화

### 목표

M1에서 "세력이 게임에 영향을 주기 시작"했다면, M4에서는 세력이 완결된 진행 시스템이 된다. 전용 시설을 건설하고, 거점을 방문하여 상점에서 아이템을 구매하고, 세력 전용 트레잇을 획득한다. M4가 끝나면 "어떤 세력과 손을 잡느냐"가 용병단의 장기 방향성을 결정하는 핵심 선택이 된다.

### 포함 시스템

| 시스템 | 핵심 변화 |
|--------|----------|
| **세력 전용 시설 14종** | 가입 세력마다 고유 시설 건설 가능. 탈퇴 시 잠금(레벨 보존). 기존 시설 시스템 위에 확장 |
| **세력 거점 + 상점** | 각 세력의 거점 리전 지정. 거점 방문 시 세력 상점 이용 가능. 평판 구간별 상품 범위 확장 |
| **세력 전용 트레잇** | 거점 방문 + 평판 조건 충족 시 획득 가능한 세력 고유 트레잇 |

### 선행 조건

- M1 세력 패시브/퀘스트 태그 — 평판 축적 수단이 있어야 상점/트레잇 해금에 의미 있다.
- M2a 아이템 인프라 — 상점에서 판매할 아이템이 존재해야 한다.

### 세력 전용 트레잇 데이터 모델 사전 확정

현재 `traits` 테이블과 `Mercenary.traitIds` 필드는 세력 소속 개념이 없다. M4 본 작업 진입 전에 다음을 설계 문서로 확정한다.

- `traits` 테이블 `faction_id` 필드 신설 (null = 일반 트레잇, 값 = 세력 전용).
- `traits.acquisition_condition` JSONB의 조건 평가기에 "가입 세력 일치", "거점 방문 이력", "평판 구간", "행동 지표"를 AND 결합하는 새 조건 타입 추가.
- 탈퇴 후 보유 정책 — `Mercenary.traitIds`에 세력 트레잇이 이미 들어 있으면 유지하고, `TraitAcquisitionService`는 신규 세력 트레잇 획득 시 `factionId`의 현재 가입 상태를 재확인하여 신규 획득만 차단한다.
- `TraitAcquisitionService` 확장 지점과 테스트 케이스 목록을 명세서로 선행 작성한다.

이 데이터 모델 확정이 완료되지 않으면 M4 구현을 시작하지 않는다.

### 작업 상세

**기획 (content-designer / balance-designer)**

- 14개 세력별 전용 시설 상세 설계 (이름, 설명, 최대 레벨, 비용 공식, 효과 공식).
- 기존 시설과의 시너지/중복 검토.
- 14개 세력별 거점 리전 1~2개 지정.
- 평판 구간별(11~30, 31~60, 61~90, 91~100) 상품 범위, 세력별 8~15개 상품, 가격 체계.
- 세력별 고유 트레잇 2~3종 설계 (획득 조건: 가입 + 거점 방문 + 평판 + 행동 지표).

**구현 (Flutter — band_of_mercenaries)**

세력 전용 시설:
- `FactionFacilityData` 정적 모델(Freezed).
- `FactionState.facilityLevels` (HiveField(5), 이미 존재) 활용.
- `ConstructionService` 확장 — 세력 시설도 건설 큐에 포함 (기존 큐 공유 vs 별도 큐는 기획 시점에 결정).
- `FacilityTabScreen` 확장 — 가입 세력 시설 섹션 추가 (미가입/탈퇴 시 잠금 표시).
- 세력 시설 효과를 기존 서비스에 보정값으로 주입.
- `SyncService` 확장 — `faction_facilities` 테이블 동기화.

세력 거점:
- `FactionData` 모델에 거점 리전 정보 추가.
- 이동 화면 — 거점 리전에 세력 마커 표시.
- 세력 상세 화면 — 거점 위치 + "이동" 바로가기.
- 거점 도착 판정 — 현재 리전 == 세력 거점 리전.

세력 상점:
- `FactionShopItemData` 정적 모델(Freezed): faction_id, item_id, required_reputation, price_gold.
- 거점 위치일 때 상점 UI 접근.
- 평판 구간별 상품 잠금/해금 표시, 구매 → 인벤토리 추가.

세력 전용 트레잇:
- `traits` 테이블에 세력 트레잇 추가 (기존 109개 + 28~42개). `faction_id` 필드 사용.
- `TraitAcquisitionService` 확장 — 세력 가입 + 거점 방문 + 평판 조건.
- 세력 트레잇은 거점 방문 시점에 조건을 체크한다.
- 용병 상세 오버레이 — 세력 트레잇에 세력 아이콘/색상 배지.

온보딩:
- 첫 거점 방문 시 상점 안내 튜토리얼 1회.
- 첫 세력 전용 시설 건설 가능 상태 표시 시 안내 한 줄.

**운영 도구 (operation-bom)**

- Supabase 신규 테이블 2개 — `faction_facilities`, `faction_shop_items`.
- `factions` 테이블에 거점 리전 필드 추가.
- `traits` 테이블에 `faction_id` 필드 추가.
- `table-config.ts`에 신규 테이블 정의 추가.
- `faction_shop_items` 편집 — faction_id/item_id 이름 참조, 평판 구간 셀렉트.
- `data_versions`에 신규 테이블 행 추가.

**데이터 입력 (operation-bom에서 수행)**

- 세력 전용 시설 14종 데이터.
- 14개 세력별 거점 리전 지정.
- 세력 상점 상품 — 세력당 8~15개, 총 112~210개.
- 상점 전용 아이템을 items 테이블에 추가.
- 세력 전용 트레잇 — 세력당 2~3종, 총 28~42개.
- 트레잇 관련 테이블(충돌/시너지 등)에 세력 트레잇 연관 데이터.

**밸런스 체크 (balance-designer)**

- 세력 시설 + 기존 시설 효과 합산 밸런스.
- 상점 가격 vs 골드 수급 속도.
- 세력 트레잇 강도 (가입 가치를 주되 필수가 아닌 수준).
- 충돌 세력 탈퇴(평판 -20) → 재가입 평판 회복 소요 시간.

### 누적 플레이 기준 (M4 종료 시)

- M1~M3을 거친 유저가 세력 1개에 가입하여 거점을 방문하고, 상점에서 아이템을 구매하고, 세력 전용 시설 Lv3까지 건설하고, 세력 트레잇 1개를 획득하는 10~15시간 플레이가 가능해야 한다.

### 완료 점검표

**세력 전용 시설**

- [ ] 세력 가입 후 시설 탭에 해당 세력 전용 시설이 건설 가능 상태로 표시된다.
- [ ] 전용 시설 건설/업그레이드가 기존 건설 큐에서 정상 동작한다.
- [ ] 세력 탈퇴 시 잠금, 재가입 시 레벨 보존 해금이 동작한다.
- [ ] 전용 시설 효과가 실제 게임 수치에 반영된다.

**세력 거점 + 상점**

- [ ] 이동 화면에서 세력 거점 리전에 마커가 표시된다.
- [ ] 거점 리전에 위치할 때 상점 UI에 접근할 수 있다.
- [ ] 평판 구간에 따라 상품이 잠금/해금 표시된다.
- [ ] 구매 후 아이템이 인벤토리에 추가된다.
- [ ] 거점이 아닌 리전에서는 상점에 접근할 수 없다.

**세력 전용 트레잇**

- [ ] 가입 + 거점 방문 + 평판 조건 충족 시 세력 트레잇을 획득한다.
- [ ] 탈퇴 후 보유 트레잇 유지, 신규 획득 차단이 동작한다.
- [ ] 용병 상세에서 세력 트레잇에 세력 표식이 보인다.

**데이터 & 운영 도구**

- [ ] operation-bom에서 세력 시설/상점 상품/세력 트레잇을 편집할 수 있다.
- [ ] 검증용 데이터 — 시설 14종, 거점 14개, 상점 112+개, 트레잇 28+개 입력 완료.
- [ ] data_versions 퍼블리시 후 동기화 확인.

**밸런스 & 회귀**

- [ ] balance-designer로 시설 합산/상점 가격/트레잇 강도 시뮬레이션 수행.
- [ ] M1~M3 기능 회귀 이상 없음.

### M4 완료 후 게임 상태

> 세력이 단순한 소속 표시에서 완결된 진행 시스템으로 변모한다. 모험가 길드에 가입하면 "의뢰 중개소" 전용 시설을 건설하여 고급 의뢰 확률을 높이고, 길드 거점을 방문하여 상점에서 탐험 특화 장비를 구매한다. 평판이 오를수록 더 강력한 상품과 트레잇이 해금된다. "마탑 연합과 태양 교단 중 어디를 택할 것인가" — 충돌 세력 간의 선택이 용병단의 장기 전략을 결정짓는다.

---

## M5: 용병의 유대

### 목표

M1~M4는 세계와 세력을 풍부하게 만들었다. M5는 시선을 용병 개인 간의 관계로 돌린다. 공동 파견 횟수에 따라 우정/라이벌/멘토 트레잇이 생기고, 파견 조합에 전략적 깊이가 더해진다. 티어 업그레이드는 의도적으로 M5에서 제외하여 경제 충격을 M6로 격리한다.

### 축소 근거

v1에서는 M5에 용병 상호작용, 티어 업그레이드, 이동 선택지 이벤트를 함께 넣었다. 그러나 다음 이유로 분산한다.

- 이동 선택지는 M3 서사 텍스트와 `TemplateEngine`을 공유하므로 M3으로 이동.
- 티어 업그레이드는 "골드 수급·지출 곡선을 전면 재설계한다"는 점에서 다른 기능 변경과 섞으면 밸런스 이상의 원인 분리가 불가능하므로 단독 마일스톤 취급 → M6으로 이동.
- 남은 "용병 상호작용"만으로도 트레잇 시스템·파견 화면·용병 상세에 충분한 변경이 들어간다.

### 포함 시스템

| 시스템 | 핵심 변화 |
|--------|----------|
| **용병 간 상호작용** | 공동 파견 횟수 추적 → 우정/라이벌/멘토 트레잇 자동 발생. 파견 조합에 전략적 깊이 (trait Phase C) |

### 선행 조건

- 트레잇 시스템 전체 (완료) — 관계 트레잇이 기존 엔진 위에 동작.

### 작업 상세

**기획 (content-designer / balance-designer)**

- 공동 파견 임계값, 관계 유형(우정/라이벌/멘토).
- 관계별 효과 — 같이 보낼 때 성공률 보정, 따로 보낼 때 상태 변화 등.
- 관계 트레잇 6~10종 설계.

**구현 (Flutter — band_of_mercenaries)**

- `Mercenary` 모델에 `coDispatchMap` 필드 추가 (Map<String, int>).
- `MercenaryStatService` 확장 — 퀘스트 완료 시 파견 용병 쌍별 카운트 갱신.
- `MercenaryRelationService` 신규 — 임계값 도달 시 관계 트레잇 자동 부여.
- `QuestCalculator` 확장 — 관계에 따른 성공률 보정.
- 파견 화면 — 용병 선택 시 관계 아이콘 표시.
- 용병 상세 오버레이에 "관계" 섹션 추가.

**운영 도구 (operation-bom)**

- 관계 트레잇은 기존 `traits` 테이블에 추가. 신규 테이블 없음.
- `table-config.ts` 변경 없음 (기존 traits 편집기 재사용).

**데이터 입력 (operation-bom에서 수행)**

- 관계 트레잇 6~10종 + 관계 트레잇 간 충돌/시너지 데이터.

**밸런스 체크 (balance-designer)**

- 관계 트레잇 성공률 보정이 파티 전략을 어떻게 바꾸는지 검증.
- 관계 트레잇이 부여된 후 "항상 같이 보내는 고정 파티"가 최적해가 되지 않는지 확인.

### 누적 플레이 기준 (M5 종료 시)

- 유저가 M1~M4 흐름을 거친 후 용병 2~3명을 반복 공동 파견하여 우정 트레잇을 획득하고, 해당 파티로 엘리트 토벌 성공률이 체감 가능하게 상승하는 2~3시간 추가 플레이가 가능해야 한다.

### 완료 점검표

- [ ] 공동 파견 횟수가 용병 쌍별로 누적된다.
- [ ] 임계값 도달 시 우정/라이벌/멘토 트레잇이 자동 부여된다.
- [ ] 관계에 따른 성공률 보정이 적용된다.
- [ ] 파견 화면에서 관계 아이콘이 표시된다.
- [ ] 용병 상세에 관계 섹션이 보인다.
- [ ] operation-bom에서 관계 트레잇을 편집할 수 있다.
- [ ] 관계 트레잇 6+종 입력 완료.
- [ ] balance-designer로 관계 트레잇 밸런스 검증 완료.
- [ ] M1~M4 기능 회귀 이상 없음.

### M5 완료 후 게임 상태

> 용병 김철수와 박영희는 10회 이상 함께 파견되어 우정 트레잇을 얻었다. 둘을 함께 보내면 성공률이 올라간다. 반대로 라이벌 관계가 된 용병은 같은 퀘스트에 보내면 서로 견제한다. 파견 화면에서 용병을 선택할 때 관계 아이콘이 보이며, 어떤 조합으로 보낼 것인가의 전략 레이어가 하나 더 생긴다.

---

## M6: 완성과 균형

### 목표

M1~M5에서 기능 영역을 완성했다. M6는 두 가지 작업을 한다. ① 용병 티어 업그레이드를 단독으로 구현하여 경제 곡선을 재설계한다. ② 전체 시스템의 데이터 밀도를 최종 분량까지 채우고, stub을 실제 동작으로 연결하고, 밸런스를 종합 조정하여 게임을 완성품으로 만든다. M6이 끝나면 "처음부터 끝까지 플레이해도 빈 곳이 없는" 싱글 플레이어 완성판이 된다.

### 포함 작업

| 축 | 핵심 변화 |
|----|----------|
| **용병 티어 업그레이드** | 최대 레벨 도달 용병을 상위 티어로 승급. 골드 + 정수 + 조건 필요. 경제 재설계 수반 (trait Phase E 일부) |
| **전체 데이터 채우기** | M1~M5에서 누적 분산 입력한 데이터를 최종 분량까지 완성. 리전 밀도 기준 충족 |
| **전체 밸런스 튜닝** | 티어 업그레이드 반영 후 경제/전투/성장/트레잇을 통합 시뮬레이션하여 최종 수치 확정 |
| **stub 정리 + 품질 마무리** | 미구현 시설 stub 실제 연동, 엣지 케이스 수정, 온보딩 마무리 |

### 선행 조건

- M1~M5 전체 완료.

### 티어 업그레이드를 M6로 이동한 근거

v1에서는 M5에 티어 업그레이드를 포함했다. 그러나 티어 업그레이드는 다음 속성을 갖는다.

- T1→T2~T4→T5 전체 승급에 필요한 누적 골드가 수십만~수백만 단위로 M1~M5 골드 수급 전제를 뒤흔든다.
- 승급한 용병의 인건비 상승은 파견 수지를 압박하여 M1 세력 패시브 수치 재조정을 요구할 수 있다.
- 승급 시 보유 트레잇·장비 유지 정책은 M2a/M4 데이터와 교차 검증이 필요하다.

다른 기능 변경과 섞이면 밸런스 이상이 티어 업그레이드 때문인지 다른 변경 때문인지 분리가 불가능하다. 따라서 전체 밸런스 재시뮬레이션이 이미 수행되는 M6에 단독 배치한다.

### 작업 상세

#### 축 1: 용병 티어 업그레이드

**기획 (balance-designer)**

- 승급 조건 (T1→T2 ~ T4→T5) — 최대 레벨, 골드, 정수 종류/수량, 행동 지표.
- 승급 후 변화 — 레벨 리셋, 스탯 재설정, 트레잇/장비 유지, 직업 변환 규칙, 인건비 변화.
- 85개 직업의 상위 티어 대응 매핑.

**구현 (Flutter — band_of_mercenaries)**

- `TierUpgradeService` 신규 — 승급 조건 판정, 비용 차감, 직업 전환, 스탯 재설정.
- `Mercenary` 모델 확장 — 승급 이력 필드.
- 용병 상세에 "승급" 버튼 + 조건 충족 현황 표시.
- 승급 성공 연출 — 축하 팝업 + 전/후 비교 카드 + 활동 로그.
- `SyncService` 확장 — `tier_upgrade_requirements` 테이블 동기화.

**운영 도구 (operation-bom)**

- Supabase 신규 테이블 — `tier_upgrade_requirements`.
- `table-config.ts`에 정의 추가. 티어별 조건 구조화 폼.
- `data_versions`에 행 추가.

**데이터 입력 (operation-bom에서 수행)**

- 티어 승급 조건 4단계.
- 85개 직업의 상위 티어 대응 데이터.

**밸런스 체크 (balance-designer — 최중요)**

- T1→T5 전체 비용 vs 누적 골드 수급 시뮬레이션.
- 승급 현실 소요 기간 (목표: 라이트 유저 1개월, 헤비 유저 1주).
- 승급 후 인건비 vs 순수익.
- M1~M5 수치 재조정 필요 여부 판단 → 필요 시 operation-bom에서 일괄 반영.

#### 축 2: 전체 데이터 채우기

리전별 컨텐츠 밀도 기준 (M1~M5에 분산 누적된 데이터를 기준치까지 완성):

| 리전 티어 | 리전 수 | region_discoveries | 엘리트 | 연계 퀘스트 | 변형 | 세력 단서 |
|----------|---------|-------------------|--------|-----------|------|----------|
| Tier 1 | ~60 | info 1~2개 | 없음 | 없음 | 없음~1개 | 공개 세력 태그만 |
| Tier 2 | ~50 | info 2 + elite/faction | 1종/리전 | 1~2개 체인 | 2~3개 리전 | 공개+일부 비밀 |
| Tier 3 | ~40 | info 2~3 + elite + faction + hidden_quest | 1~2종/리전 | 3~5개 체인 | 4~6개 리전 | 비밀+지역 |
| Tier 4 | ~30 | 풀 조합 3~5개 | 2종/리전 | 3~5개 체인 | 5~8개 리전 | 비밀+지역 |
| Tier 5 | ~19 | 풀 조합 4~6개 | 2~3종/리전 | 3~5개 체인 | 대부분 | 고급 세력 |

예상 최종 데이터 총량:

| 데이터 | 최종 목표 | M5 종료 시 누적 예상 | M6 추가 입력 |
|--------|---------|-------------------|-----------|
| region_discoveries | 400~600건 | 150~250건 | 250~350건 |
| elite_monsters | 150~250종 | 50~80종 | 100~170종 |
| elite_loot_tables | 450~750건 | 150~250건 | 300~500건 |
| chain_quests | 50~80 체인 (150~300단계) | 15~25 체인 | 35~55 체인 |
| quest_pools | 200(기존) + 100~200 추가 | 100~150 추가 | 50~100 추가 |
| quest_narratives | 150~200 템플릿 | 60~100 | 50~100 |
| items | 80~120종 | 30~50종 | 50~70종 |
| travel_choice_events | 30~40종 | 10~15종 | 20~25종 |
| traits | 150~170종 | 130~150종 | 20~30종 |
| faction_shop_items | 112~210건 | 112~210건 (M4에서 완결) | 없음~추가 |

작업 방식:
- content-designer로 티어별 배치 가이드라인 생성 → 기획자가 리전 단위로 검토/수정.
- 낮은 티어부터 순차 충족.
- 각 티어 그룹 완료 시 데이터 입력 → 퍼블리시 → 실제 플레이 테스트.

#### 축 3: 전체 밸런스 튜닝

경제 밸런스:
- 골드 수입 곡선 (퀘스트 + 방치 + 이벤트 + 선택지 + 승급 후 고난도 보상 총합).
- 골드 지출 곡선 (파견비 + 인건비 + 시설 + 트레잇 삭제 + 상점 + 승급 비용).
- 구간별 순 수지 (초반 F~E / 중반 D~C / 후반 B~A).
- 아이템 획득 속도 (정수 파밍 주기 vs 승급 수요).

전투 밸런스:
- 파티 전투력 vs 난이도별 적 전투력 분포.
- 상성 + 장비 + 패시브 + 관계 + 승급 합산 시 성공률 상한/하한.
- 엘리트 전투력 vs 풀파티 — 도전적이지만 불가능하지 않은 수준.

성장 밸런스:
- XP 곡선 (Lv1→Lv5 도달 기간, 티어별).
- 승급 비용 vs 골드 축적 → 현실 소요 기간.
- 명성 곡선 (F→A 도달 기간, 랭크 체류 시간 분포).

트레잇 밸런스:
- 획득 임계값, 진화 조건의 현실 달성 시점.
- 세력 트레잇 강도.
- 관계 트레잇과 세력 트레잇의 중첩 효과.

밸런스 작업 방식:
1. balance-designer로 전체 경제 시뮬레이션 (1일/1주/1개월/3개월).
2. 이상치 조정 → 재시뮬레이션 (최대 3회 반복).
3. 확정 수치를 operation-bom에서 일괄 반영.
4. 실제 플레이 테스트로 체감 검증.

#### 축 4: stub 정리 + 품질 마무리

stub 시설 실제 연동:

| 시설 | 현재 상태 | M6 작업 |
|------|----------|---------|
| 대장간 | 장비 강화 효과 stub | M2a 아이템과 연동 — 장비 강화(스탯 증가) |
| 연구소 | 특수 트레잇 해금 stub | M4 세력 트레잇 + 기존 트레잇 연동 — 고급 트레잇 조건 완화 |
| 게시판 | 특수 의뢰 알림 stub | M3 연계 퀘스트 연동 — 발견 확률 상승, 미발견 체인 힌트 |

품질 마무리:
- 전체 UI 일관성 검토.
- 활동 로그 메시지 톤 일관성.
- 엣지 케이스 수정 (승급 중 파견, 탈퇴 중 전용 퀘스트, 대량 인벤토리 성능 등).
- M3~M5에서 배치한 부분 온보딩 힌트를 전체 재검토하여 일관성 확보.
- 시설 이정표 기능 연동 (Lv5/10/15/20/25 해금).

운영 도구 (operation-bom):
- 대량 데이터 입력 효율화 — 리전 단위 일괄 편집 뷰.
- 밸런스 수치 일괄 수정 도구.
- 데이터 검증 대시보드 — 리전별 컨텐츠 채움률, 데이터 무결성 체크, 미입력 리전 하이라이트.

### 누적 플레이 기준 (M6 종료 시)

- 신규 유저가 F랭크에서 A랭크까지, Tier 1부터 Tier 5 최심부까지, 싱글 플레이 전 과정을 빈 구석 없이 플레이할 수 있어야 한다. 예상 총 플레이 시간 40~80시간.

### 완료 점검표

**티어 업그레이드**

- [ ] Lv5 용병 상세에 "승급" 버튼 + 조건 현황이 표시된다.
- [ ] 조건 충족 후 승급 시 티어↑, 레벨 리셋, 스탯 재설정이 된다.
- [ ] 승급 후 트레잇/장비가 유지된다.
- [ ] 승급 후 인건비가 새 티어 기준으로 적용된다.
- [ ] 승급 연출(팝업, 전/후 비교)이 표시된다.
- [ ] operation-bom에서 승급 조건을 편집할 수 있다.

**데이터 완비**

- [ ] 199개 리전이 티어별 밀도 기준을 충족한다.
- [ ] 엘리트 몬스터 150종+ 입력, 각각 드랍 테이블 연결.
- [ ] 연계 퀘스트 50개 체인+ 입력, 최종 보상 아이템 존재.
- [ ] 서사 템플릿이 전체 유형×결과 조합을 커버한다.
- [ ] 선택지 이벤트 30종+ 입력.
- [ ] 세력 상점 14개 세력 모두 입력.
- [ ] operation-bom 검증 대시보드에서 무결성 오류 0건.

**밸런스 확정**

- [ ] balance-designer 전체 시뮬레이션 완료 (1일/1주/1개월/3개월).
- [ ] 구간별 골드 순 수지가 건전한 성장 곡선.
- [ ] 승급 소요 기간이 설계 의도와 일치 (라이트 유저 1개월, 헤비 유저 1주 수준).
- [ ] 트레잇 획득/진화가 실제 플레이에서 체감 가능하다.
- [ ] 성공률 분포 — 쉬운 90%+ / 적정 60~80% / 도전 30~50%.

**stub 해소**

- [ ] 대장간 — 장비 강화 동작.
- [ ] 연구소 — 트레잇 조건 완화 적용.
- [ ] 게시판 — 연계 퀘스트 발견 확률 상승 + 힌트.
- [ ] 시설 이정표(Lv5/10/15/20/25) — 실제 기능 매핑.

**품질**

- [ ] M1~M5 전체 회귀 테스트 통과.
- [ ] UI 스타일 일관성 확인.
- [ ] 활동 로그 톤 일관성.
- [ ] 온보딩 힌트 자연스러움 (M3~M6 누적 분산 배치 결과 재검토).
- [ ] 엣지 케이스 정상 처리.
- [ ] 대량 데이터 성능 저하 없음.

**운영 도구 최종**

- [ ] 리전 단위 일괄 편집 뷰 동작.
- [ ] 검증 대시보드에서 채움률 확인 가능.
- [ ] 밸런스 수치 일괄 수정 가능.
- [ ] 전체 data_versions 퍼블리시 + Flutter 동기화 최종 확인.

### M6 완료 후 게임 상태

> 게임을 처음 시작한 플레이어가 Tier 1에서 용병을 모집하고, 퀘스트를 수행하며 명성을 쌓고, Tier 2로 넘어가 세력을 발견하고, 지역을 조사하여 엘리트를 해금하고, 장비를 모으고, 연계 퀘스트의 서사를 따라 대륙을 횡단하고, 세력에 가입하여 전용 시설과 상점을 이용하고, 용병을 T5까지 승급시키고, Tier 5 최심부에서 최강 엘리트에 도전하기까지 — 빈 곳 없이 연결된 하나의 완성된 경험이 된다. 모든 수치는 밸런스가 잡혀 있고, 모든 시스템은 서로 연결되어 있으며, 운영팀은 operation-bom에서 데이터를 자유롭게 조정할 수 있다.

---

## 타임라인 가이드

### 예상 작업 규모

| 마일스톤 | Supabase 신규 테이블 | Hive 변경 | Flutter 신규 서비스 | operation-bom | 상대적 규모 |
|---------|---------------------|----------|-------------------|---------------|-----------|
| M1 | 1~2개 (+quest_pools 스키마 확장) | 없음 | 2~3개 | table-config 2개 | 중 |
| M2a | 1개 | 1개 (inventory) | 3개 | table-config 1개 + 수동 지급 UI | 중~대 |
| M2b | 2개 | 없음 | 1~2개 | table-config 2개 | 중 |
| M3 | 3개 | 1개 (chainQuestProgress) | 4개 (TemplateEngine 포함) | table-config 3개 | 대 |
| M4 | 2개 | 없음 (기존 활용) | 3~4개 | table-config 2개 | 대 |
| M5 | 없음 | 모델 확장 | 1~2개 | 변경 없음 | 소~중 |
| M6 | 1개 | 없음 | stub 연동 3개 + TierUpgrade | 대시보드/일괄편집 | 특대 (티어 업그레이드 + 데이터) |

### 마일스톤별 작업 흐름 (반복)

```
1. 기획 확정
   └─ content-designer / balance-designer로 컨텐츠/수치 설계
   └─ (M4 한정) 세력 전용 트레잇 데이터 모델 사전 확정
   └─ 기획자 검토 및 확정

2. 개발 명세서 생성
   └─ spec-writer로 마일스톤별 개발 명세서 생성
   └─ verify-spec으로 검증

3. 구현
   └─ implement-spec 또는 implement-agent로 Flutter + operation-bom 동시 구현
   └─ 코드 리뷰

4. 데이터 입력
   └─ operation-bom에서 기획 확정된 데이터 입력
   └─ 누적 플레이 기준을 만족하는 분량 확보
   └─ data_versions 퍼블리시

5. 밸런스 체크포인트
   └─ balance-designer로 시뮬레이션
   └─ 이상치 수정 → 재시뮬레이션

6. 완료 점검
   └─ 마일스톤 점검표 전체 항목 확인
   └─ 이전 마일스톤 회귀 테스트
   └─ 누적 플레이 기준 실제 검증 (처음부터 해당 마일스톤까지 연속 플레이)
   └─ 다음 마일스톤 진입 결정
```

### Supabase 테이블 누적 추이

```
현재:   18개 테이블
M1 후:  20개 (+faction_passive_bonuses, 상성 테이블) + quest_pools 스키마 확장
M2a 후: 21개 (+items)
M2b 후: 23개 (+elite_monsters, elite_loot_tables)
M3 후:  26개 (+chain_quests, quest_narratives, travel_choice_events)
M4 후:  28개 (+faction_facilities, faction_shop_items) + traits 스키마 확장(faction_id)
M5 후:  28개 (신규 테이블 없음, 관계 트레잇은 traits에 추가)
M6 후:  29개 (+tier_upgrade_requirements)
```

### Hive 박스 누적 추이

```
현재:   8개 박스 (settings, user, mercenaries, quests, activityLogs, staticDataCache, regionStates, factionStates)
M2a 후: 9개 (+inventory)
M3 후:  10개 (+chainQuestProgress)
M6 후:  10개
```

---

## 기존 로드맵 문서와의 관계

이 마스터 로드맵은 아래 기존 문서들의 계획을 통합하여 대체한다.

| 기존 문서 | 상태 | 이 로드맵에서의 위치 |
|----------|------|-------------------|
| `content-design/roadmap/trait_enrichment_roadmap.md` | Phase A, B 완료 | Phase C → M5, Phase D → M3 (통합), Phase E → M6 (티어 업그레이드) |
| `content-design/roadmap/world_expansion_roadmap.md` | Phase 1, 6 완료 | Phase 2 → M2a, Phase 3 → M2b, Phase 4 → M3, Phase 5 → M3 |
| `content-design/[content]20260416_faction_system.md` | 기본 데이터 정비 완료 | 패시브 연동 → M1, 퀘스트 태그 → M1, 시설/거점/상점/전용 트레잇 → M4 |
| `content-design/20260415_next_development_priority_analysis.md` | 분석 완료 | 우선순위 기준으로 마일스톤 순서에 반영 |
| `future_ideas.md` | 아이디어 | 상성 → M1, 서사 → M3, 선택지 → M3 (이동), 명성 보너스 → M1 |

### 참고: 이 로드맵 범위 밖 (멀티플레이)

아래 항목은 이 로드맵에 포함되지 않으며, 싱글 플레이어 완성 후 별도 로드맵으로 계획한다.

- PvP / 습격 시스템
- 동맹 / 길드
- 세력 간 영향력 경쟁 (서버 연동)
- 지역 점령
- 랭킹
- 국제화 (i18n)

---

## 마일스톤 실행 가이드

각 마일스톤을 시작할 때 다음 절차를 따른다.

```bash
# 1. 기획 확정 후 개발 명세서 생성
/spec-writer @Docs/roadmap/master_roadmap.md  # M{N} 섹션 참조

# 2. 명세서 검증
/verify-spec @Docs/spec/M{N}_spec.md

# 3. 구현 실행
/implement-spec @Docs/spec/M{N}_spec.md
# 또는
/implement-agent @Docs/spec/M{N}_spec.md
```

각 마일스톤 완료 시 다음을 수행한다.

1. 해당 마일스톤의 **완료 점검표** 전 항목 확인.
2. **이전 마일스톤 회귀 테스트** 수행.
3. **balance-designer로 밸런스 스냅샷** 생성.
4. **누적 플레이 기준** 실제 검증 (처음부터 해당 마일스톤까지 연속 플레이).
5. `content_status.md` 업데이트.
6. 다음 마일스톤 진입 결정.

## 다음 단계

M1부터 순차 착수한다. M1은 즉시 착수 가능하며, M1 기획 확정 단계에서 14개 세력 패시브 매핑과 상성 매트릭스 설계를 병행 진행한다. 병렬로 M4 진입 전까지 "세력 전용 트레잇 데이터 모델 사전 확정" 설계 문서를 준비한다.
