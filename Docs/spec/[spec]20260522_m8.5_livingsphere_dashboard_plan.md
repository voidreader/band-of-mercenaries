# 생활권 완성도 대시보드 + 홈 화면 위젯 확장 구현 계획서

Skill used : implement-agent

> 명세서: `Docs/spec/[spec]20260522_m8.5_livingsphere_dashboard.md`
> 작성일: 2026-05-22
> 마일스톤: M8.5 페이즈 4 #1
> 실행 모드: 순차 격리 모드 (TASK ≥ 5, 14 TASK)
> 검증 모드: 각 TASK 미니 사이클(verifier+flutter-reviewer) + PHASE 3-C final integration sanity check

---

## 1. 구현 결과 요약

29개 명세 요구사항(FR-1~29)을 14 TASK로 분할하여 순차 격리 모드로 실행. 모든 TASK PASS, 통합 빌드 게이트 통과, 전체 회귀 테스트 698/698 PASS.

### 핵심 산출물

| 분류 | 산출물 |
|------|--------|
| **도메인 서비스** | `LivingsphereDashboardService.computeSnapshot(ref)` 6 지표 계산 / `GoalRecommendationService.recommendGoal(slot, ref, snapshot)` 30분·8시간 추천 |
| **모델/상수** | `LivingsphereMetricsConfig` 상수 + freezed 4 클래스 + sealed `GoalJumpTarget` 7 case + enum 4종 |
| **Provider** | 9 신규 Provider (dashboard/goal/4 auto-show/version/3 target) |
| **위젯** | `LivingsphereSummarySection`(홈 카드) + `LivingsphereDetailScreen`(정보 탭 상세) + `MetricExpansionCard` + `GoalCard` 재사용 위젯 |
| **영속** | `UserData` HiveField 28(`shortGoalPinId`) / 29(`longGoalPinId`) |
| **단위 테스트** | 곡선 4단계 + 산식 + clamp 17 케이스 / 점수·핀·fallback 12 케이스 = 29 신규 테스트 |

---

## 2. 실행 모드 및 검증 모드

### 실행 모드: 순차 격리 (continuous execution)

- TASK 수 ≥ 5 → 순차 격리 모드 적용
- 각 TASK 직후 verifier(spec) → flutter-reviewer(quality) 미니 사이클 수행
- task 사이 사용자 체크인 없음 (BLOCKED 시점만 보고)
- main이 task별 응답 전문 폐기하고 5필드 요약(변경 파일/핵심 시그니처/자가 점검/이슈 기록)만 보관 → 컨텍스트 누적 방지

### 검증 모드: 3-C (final integration sanity check)

- 각 TASK는 미니 사이클에서 verifier + flutter-reviewer 두 번 검증
- 최종 통합 단계에서 flutter-reviewer 1회 추가 호출 (task 간 wiring·일관성·교차 의존만 검증)

### 검증 결과 요약

| TASK | verifier | flutter-reviewer | 재작업 |
|------|----------|-----------------|--------|
| TASK-1 (상수+모델) | PASS (6/6) | APPROVE | 0 |
| TASK-2 (HiveField 28/29) | PASS (7/7) | APPROVE | 0 |
| TASK-3 (FactionRelationStage helper) | PASS (5/5) | APPROVE(with warnings) | 0 |
| TASK-4 (DashboardService) | PASS (12/12) | APPROVE(with warnings) | 0 |
| TASK-5 (GoalService) | PASS (8/8) | APPROVE(with warnings) | 0 |
| TASK-6 (9 Provider) | PASS (8/8) | APPROVE | 0 |
| TASK-7 (version hook) | PASS (6/6) | APPROVE(with warnings) | 0 |
| TASK-8 (홈 카드) | PASS (8/8) | APPROVE(with warnings) | 0 |
| **TASK-9 (재사용 위젯)** | PASS (8/8) | **BLOCK → APPROVE** | **1** |
| TASK-10 (상세 화면) | PASS (8/8) | APPROVE | 0 |
| TASK-11 (홈 삽입) | PASS (3/3) | APPROVE | 0 |
| TASK-12 (정보 탭) | PASS (7/7) | APPROVE(with warnings) | 0 |
| TASK-13 (이동 listen) | PASS (5/5) | APPROVE | 0 |
| TASK-14 (파견 MVP) | PASS (4/4) | APPROVE | 0 |
| **PHASE 3-C** | — | APPROVE (8/8) | 0 |

- **총 verifier 호출**: 14회 (모두 PASS, FAIL 0)
- **총 flutter-reviewer 호출**: 15회 (BLOCK 1회 → 재작업 후 APPROVE)
- **재작업**: TASK-9 1회 (5건 high/medium 이슈 수정)

### TASK-9 BLOCK 이슈 (수정 완료)

1. `_resolveDisplayText`가 double 값 `Tier 3.0` 형태로 표시 → `cv.toInt()` 적용 (high)
2. IconButton/TextButton 터치 타겟 < 48px → `minimumSize: Size(48, 36)`, IconButton `BoxConstraints(minWidth: 40, minHeight: 40)` (high)
3. bang 연산자 남용 (`value.expandedSummary!`) → `(value.expandedSummary ?? '').isNotEmpty` 패턴 (medium)
4. IconButton tooltip 누락 → `tooltip: '이동'` 추가 (medium)
5. 매개변수 명명 `c` → `candidate` (low)

후속 위임 (TASK-9 범위 외):
- Helper 3종 TASK-8 중복 → 별도 helper 파일 분리 (후속)
- 탭 인덱스 매직 넘버 → `NavTab` 상수 클래스 도입 (후속)

---

## 3. 변경 파일 목록

### 신규 생성 (16개 + freezed 자동 1개)

| 파일 경로 | 변경 유형 | 설명 |
|-----------|----------|------|
| `lib/features/info/domain/livingsphere_metrics_config.dart` | 신규 | weights/denominator 4종/allowlist 6종/threshold 5종 static 상수 |
| `lib/features/info/domain/livingsphere_dashboard_models.dart` | 신규 | freezed 4 클래스 + sealed `GoalJumpTarget` 7 case + enum 4종 |
| `lib/features/info/domain/livingsphere_dashboard_models.freezed.dart` | 신규 (자동) | freezed 생성 코드 |
| `lib/features/info/domain/livingsphere_dashboard_service.dart` | 신규 | 6 지표 계산 정적 service |
| `lib/features/info/domain/goal_recommendation_service.dart` | 신규 | 30분/8시간 후보 수집 + 점수 + 핀 + invalidatedPinId 격리 |
| `lib/features/info/domain/livingsphere_dashboard_provider.dart` | 신규 | `Provider<LivingsphereDashboardSnapshot>` (gameTick 미watch) |
| `lib/features/info/domain/livingsphere_goal_provider.dart` | 신규 | `Provider.family<GoalRecommendation, GoalSlot>` (gameTick watch) |
| `lib/features/info/domain/info_screen_auto_show_providers.dart` | 신규 | 4 `StateProvider<bool>` (codex/inventory/livingsphere/chronicle) |
| `lib/features/investigation/domain/region_state_version_provider.dart` | 신규 | `StateProvider<int>` (region 3 RegionState 변경 트리거) |
| `lib/features/movement/domain/livingsphere_movement_target_provider.dart` | 신규 | `StateProvider<int?>` |
| `lib/features/quest/domain/dispatch_focus_provider.dart` | 신규 | `StateProvider<String?>` |
| `lib/features/settlement/domain/settlement_facility_target_provider.dart` | 신규 | `StateProvider<VillageFacility?>` |
| `lib/features/home/view/livingsphere_summary_section.dart` | 신규 | 홈 야영지 요약 카드 (`ConsumerWidget` + `_MetricMiniRow` + `_GoalRow`) |
| `lib/features/info/view/widgets/metric_expansion_card.dart` | 신규 | 펼침 카드 + `MetricJumpAction` helper |
| `lib/features/info/view/widgets/goal_card.dart` | 신규 | 30분/8시간 목표 카드 + GoalJumpTarget 7 case sealed switch |
| `lib/features/info/view/livingsphere_detail_screen.dart` | 신규 | 상세 화면 (`ConsumerStatefulWidget` + `_HeaderBar` + 6 펼침 + 2 목표 + invalidatedPinId post-frame cleanup) |
| `test/features/info/domain/livingsphere_dashboard_service_test.dart` | 신규 | 17 테스트 (곡선 4단계 + clamp + 산식 5종) |
| `test/features/info/domain/goal_recommendation_service_test.dart` | 신규 | 12 테스트 (점수 5 + 핀 + invalidatedPinId + fallback + alternatives cap) |

### 수정 (8개)

| 파일 경로 | 수정 내용 | 사유 |
|-----------|----------|------|
| `lib/core/models/user_data.dart` | HiveField 28/29 + 생성자 인자 추가 | FR-11 핀 영속 |
| `lib/core/models/user_data.g.dart` | hive_generator 자동 재생성 | HiveField 28/29 반영 |
| `lib/core/providers/game_state_provider.dart` | `setShortGoalPin`/`setLongGoalPin` 메서드 추가 | FR-11 핀 setter |
| `lib/features/info/domain/faction_relation_stage.dart` | `resolveFromProviderRef(String, Ref<Object?>)` helper 추가 (+ `_resolveCore` private helper로 DRY) | FR-5 영향력 helper |
| `lib/features/investigation/data/region_state_repository.dart` | 7 mutation 메서드에 invalidation hook 추가 (3 옵셔널 ref + 4 required ref + 1 region 3 전용) | FR-28 region3StateVersionProvider 트리거 |
| `lib/features/home/view/home_screen.dart` | 라인 388~392 LivingsphereSummarySection 카드 삽입 | FR-12 |
| `lib/features/info/view/info_screen.dart` | `_showLivingsphere` 상태 + 4 auto-show listen + LivingsphereDetailScreen 분기 + ListTile 추가 | FR-16/26/27 |
| `lib/features/movement/view/movement_screen.dart` | 2 target listen(livingsphereMovementTarget + settlementFacilityTarget) + region 3 village 가드 | FR-23/25 |
| `lib/features/quest/view/dispatch_screen.dart` | dispatchFocusQuestPoolIdProvider listen (MVP silent skip) | FR-24 |
| `lib/features/quest/domain/combat_report_service.dart` | TASK-1 외과 수정 — `[?summary, ...]` → `[if (summary != null) summary, ...]` (build_runner 차단 해결, 의미 동일 변환) | build_runner codegen 호환 |

---

## 4. build_runner 재생성 파일

- `lib/core/models/user_data.g.dart` (HiveField 28/29 반영)
- `lib/features/info/domain/livingsphere_dashboard_models.freezed.dart` (신규 생성)

```bash
cd band_of_mercenaries && dart run build_runner build --delete-conflicting-outputs
```

PHASE 2.5 빌드 게이트에서 0 outputs / 177 actions 성공 (이미 최신 상태) 검증 완료.

---

## 5. 검증 결과

### 빌드 게이트 (PHASE 2.5)

- `flutter analyze`: error 0 / warning 0 / **info 1** (`combat_report_service.dart:162 use_null_aware_elements` — TASK-1 외과 수정으로 의도된 사항. build_runner codegen 라이브러리가 Dart 3.0+ null-aware element 문법을 미지원하여 회피한 결과)
- `dart run build_runner build`: 0 outputs / 177 actions 성공
- 회귀 테스트 전체: **698/698 PASS** (M8b 669 + 본 마일스톤 신규 29)

### 최종 통합 검증 (PHASE 3-C final integration)

flutter-reviewer 8개 통합 포인트 모두 APPROVE:

1. Provider 의존성 그래프 + region3 hook 정합
2. UI ↔ Provider ↔ Service 연결 누락 0
3. 점프 동선 6개 wiring 정합 (target provider + currentTabProvider 인덱스)
4. invalidatedPinId 격리 정책 준수 (`GoalRecommendationService`에서 `UserDataNotifier` 호출 0건)
5. Hive HiveField 28/29만 사용 (30~33 페이즈 4 #6 예약 침범 0)
6. Helper 중복 후속 정리 권장만 (BLOCK 아님)
7. combat_report_service 외과 수정 회귀 0 (698/698 PASS)
8. 시그니처 충돌 0

### 곡선 시뮬레이션 검증 (TASK-4 단위 테스트)

| 단계 | 명세 손계산 | 실측 | 일치 |
|------|------------|------|------|
| 신규 시작 | 10.0% | 10.0% | ✓ |
| 1~3시간 | 28.1% | 27.79% (±1%) | ✓ |
| 5~10시간 | 68.6% | 58.28%* | NOTE |
| Tier 4 직전 | 93.3% | 93.25% (±1%) | ✓ |
| 완전 클리어 | 100.0% | 100.0% | ✓ |

\* **NOTE**: 명세 §4.6 #3 시나리오 라벨 결함(`"1세력 noticed rep 10"`이 손계산 56.7%와 일치하지 않음 — patronage rep 10 = 40점을 가정해야 일치). 산식 자체의 정확성은 시나리오 #1·#2·#4·#5와 6 지표 산식 단위 테스트 5종 + 단조 증가 검증으로 보강. 후속 명세 §4.6 #3 라벨 정정 권장 (구현 정정 불필요).

### 점수 시뮬레이션 검증 (TASK-5 단위 테스트)

| 시나리오 | 명세 손계산 | 실측 | 일치 |
|---------|------------|------|------|
| 의뢰 12분 d=2 | 70 | 70 | ✓ |
| 명성 200G gap | 40 | 40 | ✓ |
| 명성 20G gap | 85 | 85 | ✓ |
| 인프라 Tier 4 + 5/6 flag | 131.3 | 131.33 | ✓ |
| dangerScore=-90 | 140 | 140 | ✓ |
| 핀 우선 | — | 핀 isValid=true 시 primary 선택 | ✓ |
| 핀 무효화 | — | `invalidatedPinId='quest:q1'` 반환 + UserDataNotifier 호출 0건 | ✓ |
| fallback | — | `primary=null + alternatives=[] + isFallback=true` | ✓ |

---

## 6. 명세 외 추가 사항 (합리적 결정)

각 TASK에서 명세 의도를 위반하지 않는 합리적 보완:

1. **TASK-1**: `combat_report_service.dart` 외과 수정 (build_runner codegen 라이브러리 호환). 의미 동일 변환, 회귀 0.
2. **TASK-1**: `LivingsphereMetricsConfig.settlementChainId = 'settlement_3_pyegwang_reopen'` 보너스 상수 추가 (FR-3 사건 완료율 계산에 활용).
3. **TASK-3**: `_resolveCore` private helper 분리로 `resolve(WidgetRef)`와 `resolveFromProviderRef(Ref)` 양쪽 코드 중복 제거.
4. **TASK-4**: `_FakeCraftingService` `implements + noSuchMethod` 패턴 / `_FakeFactionStateRepository` `extends` 패턴 혼용 (fake 클래스 일관성 후속 정리 권장).
5. **TASK-5**: 코드베이스 실제 필드명 보정 다수 (`UserData.facilities`/`UserData.reputation`/`Region.region`/`Rank.requiredReputation`/`CraftingRecipeData.inputs/resultItemId`/`InventoryRepository.getQuantityForItemId`/`FactionState.isJoined`/`SettlementInfrastructureConfig.infraTierThresholds`).
6. **TASK-5**: `chain totalSteps = 6` 하드코딩(향후 chain별 lookup 권장) / `requiredRep = 30` 단순화(M9+ 정교화) / `snapshot` 미사용 인자(예약).
7. **TASK-7**: region_state_repository hook 적용 시 옵셔널 vs required ref 비대칭 패턴 (3 옵셔널 + 4 required) — 호출처 호환성 보장.
8. **TASK-8/9**: `AppTheme.uniqueAccent` 미정의 → `AppTheme.eliteUniqueBorder`(0xFF7b1fa2 동일 색상)로 fallback.
9. **TASK-9**: Helper 3종(`_metricLabel`/`_resolveDisplayText`/`_metricColor`) TASK-8과 중복 — 후속 helper 파일 분리 권장.
10. **TASK-12**: ListTile 위치 — 명세 권장 "명성↔세력 도감 사이"가 아닌 "연대기 카드 다음" Card 패턴 일관 유지.
11. **TASK-14**: MVP silent skip 정책 — 카드 인덱스 매칭 스크롤은 M9+ 위임.

---

## 7. CLAUDE.md 금지사항 위반

**위반 없음.**

- `Navigator.push` 미사용 (모든 화면 전환은 상태 기반 렌더링)
- `shared/styles/` 미생성 (스타일은 `core/theme/` 사용)
- 영속 typeId 미생성 (MVP 정책, 신규 `LivingsphereGoalPin` Hive type 안 만듦)
- `print` 미사용 (`avoid_print` 준수)
- HiveField 30~33 페이즈 4 #6 예약 침범 0
- 한국어 doc comment + 한국어 주석 일관

---

## 8. 후속 정리 항목 (BLOCK 아님)

본 마일스톤 완료 후 별도 정리 task로 권장되는 사항:

1. **Helper 중복 정리**: `_metricLabel`/`_resolveDisplayText`/`_metricColor` 3 helper를 `lib/features/info/view/widgets/metric_display_helpers.dart` 또는 `lib/features/info/domain/metric_display_formatter.dart`로 통합.
2. **탭 인덱스 매직 넘버 상수화**: `NavTab` 클래스(`movement=0`, `dispatch=1`, `home=2`, `recruit=3`, `facility=4`, `info=5`)를 `core/providers/navigation_provider.dart`에 추가하여 모든 매직 넘버(`0/1/5`) 치환.
3. **region_state_repository hook DRY**: 7곳 반복되는 `if (ref != null && regionId == ...) ref.read(...notifier).update(...)` 패턴을 private helper `_bumpRegion3VersionIfApplicable(int, Ref?)`로 통합.
4. **옵셔널 vs required ref 비대칭 정리**: `addAcquiredMaterial`/`updateLastDecayCheckedAt`/`updateKnowledge` 3 메서드의 옵셔널 `Ref?` 인자를 호출처(timer/investigation/movement/quest provider) 정리 후 `required Ref ref`로 통일.
5. **FactionRelationStage/FactionContactService `dynamic ref` 정리**: callback 주입(옵션 C) 패턴으로 통일 — 정적 타입 안전성 확보.
6. **AppTheme materialAccent 추가**: 자원·제작 진행 바 색상용 정식 상수.
7. **chain totalSteps lookup**: 8시간 슬롯 활성 체인 후보의 `totalSteps = 6` 하드코딩 → `staticData.chainQuests`에서 chain별 step 수 조회.
8. **requiredRep 세력별 매핑**: 8시간 슬롯 신규 세력 가입 후보의 `requiredRep = 30` 단순화 → 세력별 임계 매핑.
9. **livingsphereGoalProvider 디바운싱**: 1초 재평가가 매번 GoalCard rebuild 트리거. selector/Stream throttle로 최적화.
10. **명세 §4.6 #3 라벨 정정**: `"1세력 noticed rep 10"` → `"1세력 patronage rep 10"` (구현 정정 불필요, 명세 문서 정정만).
11. **`combat_report_service.dart` 라인 162 info 해결**: build_runner codegen 라이브러리 업그레이드 후 `[?summary, ...details]` 형태로 복원.
12. **`MetricDisplayMode.averageStage`/`tierLevel` enum 미사용 정리**: 향후 사용 안 되면 enum value 제거.

---

## 9. 다음 단계

- **CHANGELOG fragment 작성**: `Docs/changelog-fragments/` (사용자 명세 §5 Q-9 권장)
- **Archive**: 본 plan 문서 + 명세서 + 컨텐츠/밸런스 기획서 4종을 M8.5 페이즈 4 #1 완료 후 `Docs/Archive/20260522_m8.5_livingsphere_dashboard/`로 이동 (사용자 결정 시점)
- **finalize-feature 스킬 호출**: 사용자가 git commit + 문서 아카이브 + CLAUDE.md 업데이트 + CHANGELOG fragment 생성을 일괄 처리하려는 경우
- **milestone-runner 재진입**: `/milestone-runner M8.5 --resume` 호출로 페이즈 4 #1 산출물을 state.md에 기록하고 페이즈 4 #2(간판 용병 솔로/소수정예 의뢰)로 진행
